@using BSDSalzburg2024.Application.Requests.Models

@inject IStringLocalizer<SexDisplay> localizer

<span>@this.localizer.GetString(this.Model.Character.ToString())</span>

@code {
    [Parameter, EditorRequired]
    public Sex Model { get; set; }
}
